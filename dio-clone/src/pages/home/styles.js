import styled from "styled-components";
import {
    Container as BaseContainer,
    Title,
    TitleHighlight,
    TextContent,
    Column
} from "../styles";

export const Container = styled(BaseContainer)`
    margin-top: 150px;
`;

export { Title, TitleHighlight, TextContent, Column };
